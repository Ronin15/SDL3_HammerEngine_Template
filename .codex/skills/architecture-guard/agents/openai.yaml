interface:
  display_name: "Architecture Guard"
  short_description: "Check and enforce engine architecture"
  default_prompt: "Use $architecture-guard to review this change for EDM, manager, controller, and state-boundary coherence."

policy:
  allow_implicit_invocation: true
